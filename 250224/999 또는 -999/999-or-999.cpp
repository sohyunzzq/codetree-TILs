#include <iostream>
using namespace std;

int main() {
    int min_val=1000;
    int max_val=-1000;

    while(1){
        int n;
        cin>>n;

        if(n==999 || b==-999)
        break;

        if(n>max_val)
        max_val=n;

        if(n<min_val)
        min_val=n;
    }

    cout<<max_val<<" "<<min_val;
    // Please write your code here.
    return 0;
}